version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: PgPassword
      POSTGRES_USER: PgUser 
      POSTGRES_DB: market
    ports:
      - 5432:5432
